# ブラウザアプリのデモ

video_app/
│── app.py             # バックエンド (Flask)
│── static/
│   ├── style.css      # CSS
│   └── script.js      # JavaScript
│── templates/
│   └── index.html     # フロントエンド (HTML)
│── uploads/           # アップロードされた動画
│── processed/         # 加工後の動画
